---

name: Thingworx Application
on:
  push:
    branches:
      - 'dev'
    paths:
       - '.github/workflows/thingworx_application.yml'
  pull_request:
    branches:
      - 'qa'
      - 'release'
    
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      working-directory: ./Thingworx-Application/
    steps:
      - uses: actions/checkout@v2

